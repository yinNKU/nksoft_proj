# 南开大学 软件工程大作业 —— 团队作业要求

---

## 一、课程基本信息

- **课程**：软件工程大作业
- **学校**：南开大学（Nankai University）
- **提交形式**：团队作业

---

## 二、项目概述

开发一个面向单细胞高维向量数据的近似最近邻（ANN）检索系统，为大规模单细胞数据提供高效的相似样本检索能力。

系统支持以下功能：

- 单细胞数据的读取与预处理
- 精确检索与近似检索
- 多种索引结构管理
- 查询结果展示
- 性能评测

用户可以通过 Web 页面输入查询细胞或查询向量，设置检索参数，获取 top-k 相似结果，并查看查询耗时、索引状态和评测指标等信息。

---

## 三、背景知识

### 3.1 单细胞测序技术

把组织中的单个细胞分离出来，分别测量每个细胞内部的分子信息，再通过测序读出这些信息。测序完成后，可以统计每个细胞中各个基因的表达量，从而得到一个"细胞 × 基因"的表达矩阵。

> **输出**：从每个分区细胞得到的数字基因表达和细胞表面蛋白表达或 CRISPR 扰动图谱。

### 3.2 近似最近邻检索（ANN）

随着单细胞测序技术的发展，单细胞数据的规模不断扩大，一个实验往往可以产生数十万个细胞样本。经过数值化表示后，每个细胞都可以看作一个高维向量。

传统的精确最近邻搜索方法虽然能够返回准确结果，但在数据规模较大、维度较高的情况下，往往存在以下问题：

- 查询效率低
- 响应时间长
- 计算资源消耗大

近似最近邻检索（Approximate Nearest Neighbor, ANN）技术能够在保证较高检索质量的同时显著降低查询开销，已广泛应用于图像搜索、推荐系统、语义检索等场景，也适合用于单细胞数据。

---

## 四、功能模块

### 4.1 数据管理模块

负责单细胞数据的导入、读取和组织管理。系统将单细胞样本表示为高维向量，并对输入数据进行格式校验和基础预处理，以保证后续检索流程能够正常进行。

### 4.2 索引构建模块

负责根据输入数据建立检索索引，并完成索引的构建、保存和加载操作，为高效查询提供支持。

### 4.3 查询检索模块（核心模块）

负责接收用户输入的查询细胞编号或查询向量，按照设定的参数执行相似性检索，并返回 top-k 个最相近的结果。支持以下检索参数设置：

- 索引类型
- 距离度量方式
- 返回结果数量

### 4.4 可视化展示模块

负责将检索结果以直观的形式展示给用户。

### 4.5 用户信息模块

- 用户可以通过账号密码注册登录系统并查看可视化系统
- 管理员可以进行用户管理

---

## 五、数据格式说明（AnnData）

在 Python 单细胞分析生态中，最常用的数据结构为 **AnnData**，文件后缀为 `.h5ad`，主要包含以下内容：

| 字段 | 含义 |
|------|------|
| X | 基因表达矩阵（核心数据） |
| obs | 细胞元信息 |
| var | 基因元信息 |
| obsm | 降维后的特征（PCA、UMAP） |
| layers | 不同处理阶段的数据 |

> **X 的结构**：行 = 细胞（cell），列 = 基因（gene），矩阵中的值表示基因表达量。

```python
import scanpy as sc
adata = sc.read_h5ad("sample.h5ad")
print(adata.X.shape)
```

---

## 六、数据处理与降维

### 6.1 常见处理流程

```
原始表达矩阵 → 质量控制(QC) → 数据标准化 → 对数变换 → 高变基因筛选 → 数据缩放 → PCA降维 → 生成低维细胞向量
```

> 本次作业提供的数据已经进行了处理。如需其他处理，可在原始数据上自行操作。

### 6.2 降维方法

- **默认方法**：PCA
- **可尝试其他方法**：自编码器、scVI 等
- ⚠️ 注意：盲目使用其他方法生成 embedding 未必会让效果更好，请斟酌效果后使用。

---

## 七、ANN 算法与检索库

### 7.1 常见 ANN 算法

| 类型 | 代表算法 |
|------|----------|
| 哈希方法 | LSH |
| 聚类索引 | IVF |
| 图结构 | HNSW、NSG |
| 向量压缩 | PQ、OPQ |
| 混合结构 | IVF+PQ、IVF+HNSW |

**算法简介：**

- **IVF（Inverted File Index）**：先聚类，再在局部区域搜索
- **HNSW（Hierarchical Navigable Small World）**：基于图结构构建近邻网络，高召回率，查询效率高，当前主流 ANN 方法之一
- **PQ（Product Quantization）**：对向量进行压缩编码，降低内存占用，适合超大规模数据

### 7.2 常用 ANN 检索库

| 检索库 | 主流算法 | 特点 |
|--------|----------|------|
| FAISS | IVF / PQ / HNSW | Facebook AI Research 提供，支持 GPU 加速，适合大规模向量搜索 |
| HNSWLIB | HNSW | 轻量级，查询速度快，内存效率高，部署简单 |
| Qdrant（向量数据库） | HNSW | 支持元数据管理等数据库功能 |

> **向量数据库** = ANN 检索 + 数据库系统。常见的还有 Milvus、Weaviate、ChromaDB 等。

---

## 八、开发要求

- 系统必须为 **Web 应用**
- 技术栈任选（没有 Web 开发经验建议使用 **Flask**）
- 必须使用 **Git** 进行代码版本管理

---

## 九、中期提交要求

### 9.1 功能要求

- ✅ 单细胞数据读取
- ✅ 数据向量化表示
- ✅ ANN 索引构建
- ✅ 相似细胞检索
- ✅ 至少实现 **一种 ANN 算法或检索库**
- ✅ 支持 **Top-K 相似细胞搜索**，并返回对应细胞信息

### 9.2 展示安排

| 项目 | 详情 |
|------|------|
| 展示时间 | 6月1日 和 6月8日（两次课） |
| 展示顺序 | 按组号排序 |
| 展示时长 | 每组 5-8 分钟 |
| 展示内容 | 已完成功能讲解 + 界面演示（现场/录制视频均可） |
| 代码提交 | GitHub 仓库，需有提交记录，体现项目完成过程 |

### 9.3 评分规则

- **项目完成度**：15 分
- **展示**：5 分
- **小组得分** = 给分 × 小组人数（如 17×5 或 18×4）
- **个人得分** = 贡献度 × 小组得分（如 0.21 × 17 × 5）
- ⚠️ **个人得分上限为 20 分**，即使超出也只取上限
- 组长分配贡献度需使组员知情并同意

---

## 十、结项提交要求

### 10.1 必做项

| 编号 | 要求 |
|------|------|
| 1 | **系统功能完整**：完成一个可运行的单细胞 ANN 检索系统 |
| 2 | **条件检索**：系统应能根据输入细胞和条件要求（如限定某具体细胞类型）返回 Top-K 相似细胞 |
| 3 | **实验评估**：需对系统性能进行分析，包括具体的响应时间等 |
| 4 | **可视化展示**：需提供至少一种可视化结果，鼓励提供简单交互方式（如在可视化结果上直接进行查询） |
| 5 | **数据集管理**：系统要求可以进行数据集的管理（增加、删除等），并同时进行动态的索引管理 |

### 10.2 加分项（上限 2 分/人）

| 加分方向 | 说明 |
|----------|------|
| ANN 算法改进 | 对现有算法进行改进，在检索精度/查询时间/内存占用等方面获得改进效果 |
| 多数据集联合检索 | 支持多个单细胞数据集联合建立索引，实现跨数据集细胞搜索 |
| RAG + 单细胞数据库 | 自然语言查询细胞，AI 辅助细胞分析 |
| 其他功能 | 基础要求以外的其他功能，视完成情况与实现难易程度加分 |

> 加分是在总得分基础上加分，上限 2 分（每人）。

### 10.3 文档要求

需提交完整的软件开发文档，包含以下章节：

**一、项目概述**

- 开发背景
- 项目目标
- 开发环境
- 可行性分析
- 项目计划

**二、需求分析与系统设计**

- 需求分析
- 系统设计
- 详细设计
- 数据库设计
- UI 设计

**三、系统测试**

- 测试环境
- 功能测试
- 性能测试

**四、项目管理**

- 参与人员及分工
- 项目进展记录
- 项目管理工具

**五、用户手册**

### 10.4 提交方式

- **提交内容**：源代码、开发文档、演示视频
- **提交平台**：GitHub（完整前后端代码 + 安装运行说明 + 足够代码注释）
- **截止时间**：**2024 年 7 月 12 日 24:00**
- 如需提前打分可联系助教
