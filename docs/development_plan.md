# 后续开发计划

## 第一阶段：跑通最小闭环

目标：能在浏览器中输入细胞编号并返回 Top-K。

需要完成：

1. 准备 `data/sample.h5ad`。
2. 确认 `src/data_loader.py` 能正常读取数据。
3. 启动 Flask。
4. 调用 `/api/status`。
5. 调用 `/api/search`。

## 第二阶段：完善 ANN 索引

目标：能比较 HNSW、IVF、Flat 三种方案。

需要完成：

1. 确认 Flat 自检结果：查询自身必须排第一。
2. 实现 HNSW 参数调节。
3. 实现 IVF 参数调节。
4. 记录不同索引的查询耗时。

## 第三阶段：完善前端展示

目标：页面可用于中期展示。

需要完成：

1. 显示系统状态。
2. 显示查询耗时。
3. 显示 Top-K 表格。
4. 展示元信息字段。
5. 增加错误提示。

## 第四阶段：实验报告材料

目标：形成可写入报告的内容。

需要完成：

1. 架构图。
2. 数据处理流程图。
3. API 截图。
4. 检索结果截图。
5. Git 提交记录截图。
