# 如今进度 0520 1111

记录时间：2026-05-20 11:11

## 一、已测试结果

本次已执行测试命令：

```powershell
python -m pytest tests\test_data_loader.py tests\test_ann_engine.py -q -p no:cacheprovider
```

测试结果：

```text
4 passed in 0.04s
```

## 二、已实现功能

### 1. 数据加载与预处理

对应文件：

- `src/data_loader.py`

已实现：

- `load_h5ad()`：读取 `.h5ad` 文件，并检查文件是否存在、后缀是否正确。
- `get_or_create_pca_vectors()`：优先读取 `adata.obsm["X_pca"]`；如果不存在，则执行基础 scanpy 预处理并计算 PCA。
- `l2_normalize()`：对向量进行 float32 转换和 L2 归一化。
- `load_and_preprocess()`：兼容根目录旧版 `data_loader.py` 的调用形式，返回向量和 AnnData。
- `prepare_dataset()`：返回 `vectors, adata, metadata`，供服务层初始化使用。
- `extract_cell_metadata()`：从 `adata.obs` 提取细胞元信息，并补充 `cell_id`。
- `get_metadata()`：按细胞下标返回单个细胞的 JSON 友好元数据。
- `get_available_metadata()`：返回可用元数据字段列表。
- `get_dataset_summary()`：返回细胞数、基因数、PCA 维度、元数据字段等摘要信息。

### 2. ANN 索引与检索

对应文件：

- `src/ann_engine.py`

已实现：

- `ANNEngine.build_index()`：支持 `flat`、`hnsw`、`ivf` 三类 FAISS 索引。
- `ANNEngine.search()`：支持 Top-K 相似细胞检索，并检查查询向量维度。
- `ANNEngine.save_index()`：支持将 FAISS 索引保存到本地文件。
- `ANNEngine.load_index()`：支持加载已有索引，并校验索引维度和向量维度是否一致。
- HNSW 已改为使用内积度量，和当前 L2 归一化后的余弦相似度设计一致。

### 3. 服务层集成

对应文件：

- `src/search_service.py`

已实现：

- 启动时通过 `prepare_dataset()` 加载数据并构建默认索引。
- `/api/status` 使用的 `status()` 已返回数据加载状态、数据路径、细胞数、向量维度、索引类型、数据集摘要和错误信息。
- `search_by_cell_index()`：按细胞下标检索。
- `search_by_cell_id()`：按真实 `cell_id` 检索。
- `search_by_vector()`：按自定义向量检索，并做维度校验和归一化。
- `metadata_columns()`：返回可用元数据字段，供 API 使用。

### 4. Flask API

对应文件：

- `app.py`

已实现：

- `/api/status`：返回当前数据和索引状态。
- `/api/search`：支持 `id`、`cell_id`、`vector` 三种检索模式。
- `/api/metadata`：返回可用元数据字段；数据未加载时返回明确错误。

### 5. 测试

对应文件：

- `tests/test_ann_engine.py`
- `tests/test_data_loader.py`

已实现：

- ANN Flat 检索自身时应排第一。
- ANN HNSW 可以成功建索引并返回指定数量结果。
- `l2_normalize()` 可以正确处理普通向量和零向量。
- 元数据辅助函数可以返回可用字段、单细胞元数据和数据集摘要。


## 三、未实现功能与对应需要修改的文件

### 1. 真实 `.h5ad` 数据集验证

当前状态：

- 代码路径已经打通，但仓库当前没有 `data/sample.h5ad`，所以只能验证缺失数据时的 API 行为。

需要修改或补充：

- `data/sample.h5ad`：放入真实数据文件，或通过环境变量 `SC_DATA_PATH` 指向真实路径。
- `src/data_loader.py`：根据真实数据字段确认 `obs`、`var`、`obsm` 的字段结构和展示字段。
- `tests/test_data_loader.py`：增加基于小型测试 `.h5ad` 的读取和 PCA 流程测试。

### 2. 大规模数据预处理优化

当前状态：

- 已支持基础 scanpy 预处理和 PCA。
- 尚未针对超大规模稀疏矩阵做内存优化。

需要修改：

- `src/data_loader.py`：优化稀疏矩阵处理、避免不必要的 dense 转换。
- `config.py`：增加预处理参数配置，例如 `n_top_genes`、是否强制重算 PCA。

### 3. 索引参数配置化

当前状态：

- HNSW 的 `M=32`、`efSearch=64` 固定写在代码里。
- IVF 的 `nlist`、`nprobe` 由数据量自动估计。

需要修改：

- `config.py`：增加 HNSW 和 IVF 参数。
- `src/ann_engine.py`：让 `build_index()` 接收或读取这些参数。
- `scripts/build_index.py`：命令行支持传入索引参数。

### 4. 索引缓存与元信息保存

当前状态：

- 可以保存和加载 FAISS 索引文件。
- 尚未保存索引元信息，例如索引类型、维度、构建参数、数据文件版本。

需要修改：

- `src/ann_engine.py`：保存和读取索引 metadata。
- `scripts/build_index.py`：构建索引时同时输出 metadata 文件。
- `src/search_service.py`：启动时优先加载可用缓存索引，避免每次重建。

### 5. 运行时数据加载和重建索引 API

当前状态：

- 应用启动时加载数据。
- 尚未支持运行时切换数据文件或主动重建索引。

需要修改：

- `app.py`：新增 `/api/load-data`、`/api/rebuild-index`。
- `src/search_service.py`：增加重新加载数据和重建索引的方法。
- `static/js/main.js`：前端增加触发按钮和状态刷新逻辑。

### 6. 前端功能完善

当前状态：

- 后端 API 已有基础能力。
- 前端还需要进一步接入元数据、错误提示和结果展示细节。

需要修改：

- `templates/index.html`：增加元数据字段显示区域或筛选控件。
- `static/js/main.js`：接入 `/api/metadata`，完善搜索请求和错误展示。
- `static/css/style.css`：完善结果表格和状态提示样式。

### 7. 检索性能统计

当前状态：

- 尚未记录索引构建耗时、查询耗时、不同索引类型的对比结果。

需要修改：

- `src/search_service.py`：记录每次查询耗时。
- `src/ann_engine.py`：记录索引构建耗时。
- `app.py`：在 API 响应中返回耗时信息。
- `tests/`：增加耗时字段存在性的测试。

### 8. 更完整的测试覆盖

当前状态：

- 已有数据辅助函数和 ANN 基础测试。
- 尚未覆盖 Flask API、SearchService、索引保存加载、错误输入边界。

需要修改：

- `tests/test_data_loader.py`：增加真实或模拟 AnnData 的 PCA 创建流程测试。
- `tests/test_ann_engine.py`：增加 IVF、保存/加载索引、非法维度、非法索引类型测试。
- `tests/test_search_service.py`：新增服务层检索、错误处理测试。
- `tests/test_app.py`：新增 Flask API 测试。

