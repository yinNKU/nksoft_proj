# 单细胞 ANN 检索系统 —— 中期操作手册

---

## 一、中期目标

根据实验要求第九条，中期需完成以下 **6 项**：

| # | 要求 | 含义 |
|---|------|------|
| 1 | 单细胞数据读取 | 能加载 `.h5ad` 文件 |
| 2 | 数据向量化表示 | 将每个细胞表示为一个数值向量 |
| 3 | ANN 索引构建 | 对向量集建立近似最近邻索引 |
| 4 | 相似细胞检索 | 给定查询细胞，返回最相似的细胞 |
| 5 | 至少一种 ANN 算法/库 | FAISS / HNSWLIB / ... 任选 |
| 6 | Top-K 搜索 + 细胞信息 | 返回 K 个最相似结果，附带细胞元信息 |

> 一句话概括：**加载数据 → 提取向量 → 建索引 → 查 Top-K → 网页展示**，能跑通即可。

---

## 二、实现思想

### 2.1 核心流程

```
.h5ad 文件 → PCA 降维向量 → L2 归一化 → FAISS 建索引 → Web 查询 → 返回 Top-K
```

### 2.2 关键设计决策

**为什么选 FAISS？**
- 中期只需实现一种库，FAISS 是最稳妥的选择：Meta 维护、文档多、内置 HNSW/IVF 等多种索引、安装简单（`pip install faiss-cpu`）。

**为什么用 PCA 而不是原始基因表达矩阵做向量？**
- 原始基因维度高（20000+），直接影响检索速度和索引质量。PCA 降到 50 维后，既保留主要变异信息，又大幅降低计算开销。

**为什么做 L2 归一化？**
- 归一化后向量内积 = 余弦相似度。FAISS 的 `IndexFlatIP`（内积索引）可直接用于余弦检索，速度快。

### 2.3 系统架构（中期简化版）

```
┌──────────────────────┐
│   index.html (前端)   │   输入细胞编号 / K值 / 索引类型
└──────────┬───────────┘
           │ fetch('/api/search')
┌──────────▼───────────┐
│   app.py (Flask)      │   路由: /  /api/search  /api/status
└──────────┬───────────┘
           │
┌──────────▼───────────┐
│   engine.py           │   FAISS 索引构建 / Top-K 查询
└──────────┬───────────┘
           │
┌──────────▼───────────┐
│   data_loader.py      │   读 .h5ad → PCA → float32 归一化向量
└───────────────────────┘
```

---

## 三、实现步骤

### 第 1 步：环境搭建

```bash
# 在 soft_proj/ 目录下
python -m venv venv
venv\Scripts\activate          # Windows
pip install flask scanpy anndata faiss-cpu numpy pandas
```

**依赖清单：**

| 包 | 用途 |
|----|------|
| flask | Web 服务 |
| scanpy + anndata | 读取 `.h5ad`，PCA 降维 |
| faiss-cpu | ANN 索引构建与检索 |
| numpy / pandas | 数组与表格处理 |

### 第 2 步：data_loader.py —— 数据读取与向量化

```python
import scanpy as sc
import numpy as np

def load_and_preprocess(h5ad_path, n_pcs=50):
    """
    读取 .h5ad，返回 (N, n_pcs) 的 float32 归一化向量 + AnnData 对象。
    如果数据中已有 X_pca 则直接用；否则自动跑标准化→高变基因→PCA 流程。
    """
    adata = sc.read_h5ad(h5ad_path)

    if 'X_pca' not in adata.obsm:
        sc.pp.normalize_total(adata, target_sum=1e4)
        sc.pp.log1p(adata)
        sc.pp.highly_variable_genes(adata, n_top_genes=2000)
        adata = adata[:, adata.var.highly_variable]
        sc.pp.scale(adata)
        sc.tl.pca(adata, n_comps=n_pcs)

    vectors = adata.obsm['X_pca'][:, :n_pcs].astype(np.float32)

    # L2 归一化：使内积等价于余弦相似度
    norms = np.linalg.norm(vectors, axis=1, keepdims=True)
    norms[norms == 0] = 1
    vectors = vectors / norms

    return vectors, adata
```

### 第 3 步：engine.py —— ANN 检索引擎

中期至少实现一种索引，建议选 **HNSW**（不需要训练、精度高、查询快）。

```python
import faiss
import numpy as np

class ANNEngine:
    def __init__(self):
        self.index = None
        self.vectors = None
        self.index_type = None

    def build_index(self, vectors, index_type='hnsw'):
        self.vectors = vectors
        d = vectors.shape[1]

        if index_type == 'flat':
            # 精确检索（基准对比用）
            self.index = faiss.IndexFlatIP(d)

        elif index_type == 'ivf':
            nlist = int(np.sqrt(vectors.shape[0]))
            quantizer = faiss.IndexFlatIP(d)
            self.index = faiss.IndexIVFFlat(quantizer, d, nlist)
            self.index.train(vectors)
            self.index.nprobe = 10

        elif index_type == 'hnsw':
            # 推荐：无需训练，精度高，速度快
            self.index = faiss.IndexHNSWFlat(d, 32)

        self.index.add(vectors)
        self.index_type = index_type

    def search(self, query_vector, k=10):
        query = np.array(query_vector, dtype=np.float32).reshape(1, -1)
        distances, indices = self.index.search(query, k)
        return distances[0], indices[0]

    def save_index(self, path):
        faiss.write_index(self.index, path)

    def load_index(self, vectors, path):
        self.vectors = vectors
        self.index = faiss.read_index(path)
```

### 第 4 步：app.py —— Flask 后端

```python
from flask import Flask, render_template, request, jsonify
from data_loader import load_and_preprocess
from engine import ANNEngine
import numpy as np

app = Flask(__name__)
engine = ANNEngine()
adata = None
cell_ids = None

@app.before_first_request
def init():
    global adata, cell_ids
    vectors, adata = load_and_preprocess('data/sample.h5ad', n_pcs=50)
    cell_ids = adata.obs_names.tolist()
    engine.build_index(vectors, index_type='hnsw')

@app.route('/')
def index():
    return render_template('index.html')

@app.route('/api/search', methods=['POST'])
def search():
    data = request.json
    mode = data.get('mode', 'id')
    k = int(data.get('k', 10))
    idx_type = data.get('index_type', 'hnsw')

    if engine.index_type != idx_type:
        engine.build_index(engine.vectors, index_type=idx_type)

    if mode == 'id':
        query_vec = engine.vectors[int(data['cell_index'])]
    else:
        query_vec = np.array(data['vector'], dtype=np.float32)

    distances, indices = engine.search(query_vec, k=k)

    results = []
    for rank, (dist, idx) in enumerate(zip(distances, indices)):
        idx = int(idx)
        item = {'rank': rank + 1, 'index': idx,
                'cell_id': str(cell_ids[idx]), 'similarity': float(dist)}
        for col in adata.obs.columns:
            val = adata.obs.iloc[idx][col]
            if hasattr(val, 'item'):
                val = val.item()
            item[col] = str(val)
        results.append(item)

    return jsonify({'results': results, 'index_type': engine.index_type})

@app.route('/api/status')
def status():
    return jsonify({
        'n_cells': engine.vectors.shape[0] if engine.vectors is not None else 0,
        'n_dims': engine.vectors.shape[1] if engine.vectors is not None else 0,
        'index_type': engine.index_type,
    })

if __name__ == '__main__':
    app.run(debug=True, port=5000)
```

### 第 5 步：templates/index.html —— 前端页面

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <title>单细胞 ANN 检索系统</title>
    <style>
        * { box-sizing: border-box; margin: 0; padding: 0; }
        body { font-family: -apple-system, sans-serif; max-width: 900px; margin: 0 auto; padding: 20px; }
        h1 { margin-bottom: 20px; }
        .section { background: #f5f5f5; border-radius: 8px; padding: 16px; margin-bottom: 16px; }
        label { display: inline-block; width: 100px; font-weight: bold; margin: 4px 0; }
        input, select, button { padding: 6px 12px; margin: 4px 0; border: 1px solid #ccc; border-radius: 4px; }
        button { background: #4a90d9; color: #fff; border: none; cursor: pointer; font-size: 14px; }
        button:hover { background: #357abd; }
        table { width: 100%; border-collapse: collapse; margin-top: 12px; }
        th, td { padding: 8px 12px; border: 1px solid #ddd; text-align: left; font-size: 13px; }
        th { background: #eee; }
        #status-bar { font-size: 12px; color: #666; margin-top: 8px; }
    </style>
</head>
<body>
    <h1>单细胞 ANN 检索系统</h1>

    <div class="section">
        <h3>查询设置</h3>
        <div>
            <label>查询方式:</label>
            <select id="mode">
                <option value="id">按细胞编号</option>
                <option value="vector">按自定义向量</option>
            </select>
        </div>
        <div id="id-input">
            <label>细胞编号:</label>
            <input type="number" id="cell-index" value="0" min="0">
        </div>
        <div id="vector-input" style="display:none">
            <label>查询向量:</label>
            <input type="text" id="vector" placeholder="逗号分隔，如 0.1,0.2,0.3" style="width:400px">
        </div>
        <div>
            <label>返回数量 K:</label>
            <input type="number" id="k" value="10" min="1" max="100">
        </div>
        <div>
            <label>索引类型:</label>
            <select id="index-type">
                <option value="hnsw">HNSW</option>
                <option value="ivf">IVF</option>
                <option value="flat">Flat (精确)</option>
            </select>
        </div>
        <button onclick="doSearch()">检索</button>
    </div>

    <div class="section">
        <h3>检索结果</h3>
        <table id="results-table">
            <thead><tr><th>排名</th><th>细胞 ID</th><th>相似度</th><th>元信息</th></tr></thead>
            <tbody><tr><td colspan="4" style="text-align:center;color:#999">点击检索后显示结果</td></tr></tbody>
        </table>
        <div id="status-bar"></div>
    </div>

    <script>
        document.getElementById('mode').onchange = function() {
            var isId = this.value === 'id';
            document.getElementById('id-input').style.display = isId ? '' : 'none';
            document.getElementById('vector-input').style.display = isId ? 'none' : '';
        };

        function doSearch() {
            var payload = {
                mode: document.getElementById('mode').value,
                k: parseInt(document.getElementById('k').value),
                index_type: document.getElementById('index-type').value
            };
            if (payload.mode === 'id') {
                payload.cell_index = parseInt(document.getElementById('cell-index').value);
            } else {
                payload.vector = document.getElementById('vector').value.split(',')
                    .map(function(s) { return parseFloat(s.trim()); });
            }

            var t0 = performance.now();
            fetch('/api/search', {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify(payload)
            })
            .then(function(r) { return r.json(); })
            .then(function(data) {
                var t1 = performance.now();
                var cols = Object.keys(data.results[0] || {});
                var thead = document.querySelector('#results-table thead tr');
                thead.innerHTML = '<th>排名</th>' + cols.filter(function(c) {
                    return c !== 'rank';
                }).map(function(c) { return '<th>' + c + '</th>'; }).join('');

                document.querySelector('#results-table tbody').innerHTML =
                    data.results.map(function(r) {
                        return '<tr>' + cols.map(function(c) {
                            return '<td>' + (r[c] !== undefined ? r[c] : '') + '</td>';
                        }).join('') + '</tr>';
                    }).join('');

                document.getElementById('status-bar').textContent =
                    '查询耗时: ' + (t1 - t0).toFixed(1) + ' ms | ' +
                    '索引类型: ' + data.index_type + ' | ' +
                    '返回结果: ' + data.results.length + ' 条';
            });
        }

        fetch('/api/status')
            .then(function(r) { return r.json(); })
            .then(function(s) {
                document.getElementById('status-bar').textContent =
                    '系统就绪 | 细胞数: ' + s.n_cells + ' | 维度: ' + s.n_dims +
                    ' | 当前索引: ' + s.index_type;
            });
    </script>
</body>
</html>
```

### 第 6 步：启动运行

确保目录结构如下：

```
soft_proj/
├── app.py
├── data_loader.py
├── engine.py
├── templates/
│   └── index.html
├── data/
│   └── sample.h5ad       ← 你的单细胞数据文件
└── venv/
```

```bash
python app.py
# 浏览器打开 http://127.0.0.1:5000
```

---

## 四、验证方法

### 4.1 数据读取验证

在 Python 交互环境中：

```python
from data_loader import load_and_preprocess
vectors, adata = load_and_preprocess('data/sample.h5ad')

assert vectors.dtype == np.float32, "类型应为 float32"
print(f"细胞数: {vectors.shape[0]}, 维度: {vectors.shape[1]}")
print(f"前5个细胞ID: {adata.obs_names[:5].tolist()}")
```

**通过标准：** 细胞数 > 0，维度 = 50，类型为 float32。

### 4.2 索引与检索验证（核心）

```python
from engine import ANNEngine

engine = ANNEngine()
engine.build_index(vectors, index_type='hnsw')

# 用第 0 号细胞查询自己
dist, idx = engine.search(vectors[0], k=5)
print(f"索引: {idx}, 相似度: {dist}")
print(f"自身排第一: {idx[0] == 0}")   # 必须是 True
```

**通过标准：** `idx[0] == 0` 且 `dist[0]` 接近 1.0（归一化后内积最大为 1）。

### 4.3 Web 接口验证

```bash
# 启动 flask 后，另开终端：

# 状态接口
curl http://127.0.0.1:5000/api/status

# 按编号查询
curl -X POST http://127.0.0.1:5000/api/search \
  -H "Content-Type: application/json" \
  -d '{"mode":"id","cell_index":0,"k":5,"index_type":"hnsw"}'
```

**通过标准：** 返回 200，JSON 中包含 `results` 数组，每个元素有 `rank`、`cell_id`、`similarity`。

### 4.4 浏览器验证

1. 打开 `http://127.0.0.1:5000`，确认状态栏显示细胞数和维度
2. 输入细胞编号（如 0），点击"检索"
3. 确认表格出现 Top-K 结果
4. 切换到 IVF、Flat 索引各查一次，对比耗时
5. 切换到"按自定义向量"，输入逗号分隔的向量值，验证查询

**通过标准：** 以上操作均正常，结果表格正确，耗时毫秒级。

---

## 五、中期 6 项要求逐项对应

| 要求 | 对应位置 | 验证方式 |
|------|----------|----------|
| 数据读取 | `data_loader.py` — `sc.read_h5ad()` | 4.1 |
| 向量化 | `data_loader.py` — PCA + L2 归一化 | 4.1 |
| ANN 索引构建 | `engine.py` — `build_index()` | 4.2 |
| 相似细胞检索 | `engine.py` — `search()` | 4.2 |
| 至少一种 ANN 库 | FAISS (HNSW/IVF/Flat) | 三种可选，默认 HNSW |
| Top-K + 细胞信息 | `app.py` — `/api/search` 返回带元信息的结果 | 4.3 / 4.4 |

---

## 六、展示准备清单

- [ ] `.h5ad` 数据文件放入 `data/` 目录
- [ ] `python app.py` 正常启动，无报错
- [ ] 浏览器能打开页面，状态正常显示
- [ ] 按细胞编号查询，返回正确 Top-K
- [ ] 能切换索引类型并对比查询耗时
- [ ] 准备 5-8 分钟讲解：做了什么 → 怎么做的 → 演示
- [ ] GitHub 仓库有完整提交记录
